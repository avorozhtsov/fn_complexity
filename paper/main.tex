\documentclass[11pt]{article}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{graphicx}
\usepackage{tikz-cd}
\usepackage[margin=1in]{geometry}
\usepackage{microtype}
\usepackage[hidelinks]{hyperref}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{proposition}{Proposition}
\newcommand{\sig}[1]{\left\{#1\right\}}
\newcommand{\impl}{\preccurlyeq_{\mathrm{impl}}}

\title{Exchange Rates for Finite Map Signatures\\
\large A Simplified Thermodynamic Complexity}
\author{Artem Vorozhtsov}
\date{July 2026}

\hypersetup{
  pdftitle={Exchange Rates for Finite Map Signatures},
  pdfauthor={Artem Vorozhtsov}
}

\begin{document}
\maketitle

\begin{abstract}
We associate to a surjective map between finite sets the multiset of its
fiber cardinalities.  Cartesian products turn these signatures into a simple
asymptotic resource theory.  Its exchange rate \(C(g\mid f)\) is the largest
asymptotic number of copies of \(f\) implementable per copy of \(g\).
Power sums compute this operationally defined limit, while Gibbs
energy--entropy curves provide a geometric picture.  The comparison obtained
by putting the two directed rates side by side is not transitive: the three
signatures \(\sig{5,3}\), \(\sig{3,1,1}\), and \(\sig{6,1}\) form a strict
cycle.  This is a concrete monoidal resource theory of finite deterministic
computational tasks: its one-shot convertibility resembles strong Weihrauch
reducibility, while its asymptotic exchange rate is a finite-map analogue of
tensor relative exponents and Strassen's asymptotic spectrum.
\end{abstract}

\section{Signatures and implementation}

Let \(f:X\to Y\) be onto and enumerate its output as
\(Y=\{y_1,\ldots,y_r\}\).  The \emph{fiber} over \(y_i\) is the inverse image
\[
  f^{-1}(y_i)=\{x\in X:f(x)=y_i\},
  \qquad n_i=\left|f^{-1}(y_i)\right|.
\]
Thus \(n_i\) is the number of inputs mapped by \(f\) to the output \(y_i\).
After ordering these fiber sizes by decreasing value, the \emph{signature} of
\(f\) is
\[
  \sigma(f)=(n_1,\ldots,n_r),\qquad
  n_1\geq\cdots\geq n_r\geq 1,\qquad r=|Y|.
\]
Only the signature will matter below, so maps and their signatures are denoted
by the same letters.

\begin{definition}[Implemented by]
Let \(a=(a_1,\ldots,a_r)\) and \(b=(b_1,\ldots,b_s)\) be decreasing
signatures.  We say that \(a\) is \emph{implemented by} \(b\), and write
\(a\impl b\), when \(r\leq s\) and
\[
  a_i\leq b_i\qquad(1\leq i\leq r).
\]
\end{definition}

For maps, this means that there is an injective input relabeling
\(h_{\mathrm{in}}\) and a bijection from the used output fibers to the output
of \(f\) such that
\[
  f=h_{\mathrm{out}}\circ g\circ h_{\mathrm{in}}.
\]
More precisely, put
\[
  X'_g=h_{\mathrm{in}}(X_f),
  \qquad
  Y'_g=g(X'_g).
\]
The two inclusions on the right identify the parts of \(X_g\) and \(Y_g\)
actually used by the implementation, and the following diagram commutes:
\[
\begin{tikzcd}[column sep=large,row sep=large]
  X_f
    \arrow[r,"h_{\mathrm{in}}","\cong"']
    \arrow[d,"f"']
  & X'_g
    \arrow[r,hook]
    \arrow[d,"g|_{X'_g}"]
  & X_g
    \arrow[d,"g"]
  \\
  Y_f
  & Y'_g
    \arrow[l,"h_{\mathrm{out}}"',"\cong"]
    \arrow[r,hook]
  & Y_g .
\end{tikzcd}
\]
Unused fibers of \(g\) may be discarded.  The componentwise condition above
is exactly the condition that every fiber of \(f\) can be injected into its
assigned fiber of \(g\).

The Cartesian product \(a\otimes b\) has all products \(a_i b_j\) as its
fiber sizes, with multiplicity.  We write \(a^{\otimes k}\) for the \(k\)-fold
product.
\[
  a\otimes b
  =\operatorname{sort}\{a_i b_j:1\leq i\leq r,\ 1\leq j\leq s\}.
\]

There is also a natural sum.  The disjoint union of maps
\[
  f\sqcup g:X_f\sqcup X_g\longrightarrow Y_f\sqcup Y_g
\]
has as its signature the sorted concatenation of the two signatures; we write
this as
\[
  a+b=\operatorname{sort}(a_1,\ldots,a_r,b_1,\ldots,b_s).
\]
Both operations respect implementation, and Cartesian product distributes
over this sum:
\[
  a\impl b,\ c\impl d
  \quad\Longrightarrow\quad
  a+c\impl b+d,\quad a\otimes c\impl b\otimes d,
  \qquad
  a\otimes(b+c)=(a\otimes b)+(a\otimes c).
\]
Thus signatures form the nonzero part of an ordered commutative semiring
(adjoining the empty signature gives the additive zero), with multiplicative
unit \(\{1\}\).  The sum represents a disjoint, classically labelled
alternative, whereas \(\otimes\) represents independent parallel
composition.  Only \(\otimes\) is needed in the exchange-rate definition
below.

\begin{definition}[Exchange rate]
For signatures \(f\) and \(g\), set
\[
 k_{\max}^{g\leftarrow f}(n)
 =\max\left\{k\geq0:f^{\otimes k}\impl g^{\otimes n}\right\}.
\]
The exchange rate, with the implementer written first, is
\[
  C(g\mid f)
  =\lim_{n\to\infty}\frac{k_{\max}^{g\leftarrow f}(n)}{n}.
\]
\end{definition}

The sequence \(k_{\max}\) is superadditive: implementations for \(n\) and
\(m\) copies can be tensored.  Fekete's lemma therefore gives the limit
(possibly \(+\infty\)) and identifies it with the corresponding supremum.
Thus the limit is the definition; the infimum below is a computational
characterization.

\section{Related frameworks}

The present construction is a concrete semiring resource theory: signatures
are resources, implementation is free conversion, disjoint union supplies
addition, and Cartesian product combines independent resources.  General
resource theories formulate
convertibility from freely available processes and explicitly ask for rates
between many copies, while ordered commutative monoids and semirings provide
an algebraic treatment of conversion, combination, and asymptotic rates
(see, e.g., \cite{coecke-fritz-spekkens-2016,fritz-2017}).

At the one-shot level, the factorization
\[
  f=h_{\mathrm{out}}\circ g\circ h_{\mathrm{in}}
\]
resembles strong Weihrauch reducibility of computational problems, in which a
single use of \(g\) is surrounded by computable preprocessing and
postprocessing (see \cite{brattka-gherardi-pauly-2021}).  Weihrauch theory
also has a parallel product.  The important distinction is that our finite
processors are injective or bijective on the used sets; arbitrary computable
processors would forget the fiber capacities that this paper retains.

An even closer asymptotic precedent comes from algebraic complexity.  Tensors
encode bilinear computational tasks, restriction converts one tensor to
another by linear maps, and tensor product combines independent copies.
Strassen's asymptotic spectrum characterizes large tensor powers by
multiplicative monotones (see \cite{strassen-1988}).  The relative exponent
\[
  \omega(t,s)
  =\lim_{n\to\infty}\frac1n
    \min\{m:t^{\otimes m}\text{ restricts to }s^{\otimes n}\}
\]
is the resource cost per target copy and has been used to quantify
irreversibility in matrix-multiplication methods
(see \cite{christandl-vrana-zuiddam-2021}).  With the direction reversed, it
is the reciprocal convention to \(C(t\mid s)\).  In our setting the
multiplicative quantities \(Z_a(\beta)\), or their additive logarithms, play
the role of spectral monotones.  The resulting infimum over power sums is
also close to the exact asymptotic-majorization rate, expressed as a minimum
of ratios of R\'enyi entropies (see \cite{jensen-2019}).

Finally, channel simulation surrounds repeated uses of a resource channel by
an encoder and decoder in order to reproduce a target channel.  For
point-to-point discrete memoryless channels with common-randomness assistance,
the first-order interconversion rate is the ratio of channel capacities
(see \cite{cao-et-al-2024}).  Appendix~A explains why its unrestricted
finite-map variant has exactly the corresponding logarithmic
output-cardinality form.

\section{Power sums and Gibbs curves}

For a signature \(a=(a_i)\), define its power-sum partition function
\[
  Z_a(\beta)=\sum_i a_i^\beta,\qquad \beta\geq0,
\]
where \(\beta=1/T\) is inverse physical temperature.  Throughout this paper,
\(T\) denotes temperature and never inverse temperature.
The sum and product of signatures become the ordinary sum and product of
partition functions:
\[
  Z_{a+b}(\beta)=Z_a(\beta)+Z_b(\beta),
  \qquad
  Z_{a\otimes b}(\beta)=Z_a(\beta)Z_b(\beta).
\]
The fibers of the \(k\)-fold Cartesian power \(a^{\otimes k}\) are indexed by
\((i_1,\ldots,i_k)\) and have sizes \(a_{i_1}\cdots a_{i_k}\).  Consequently,
\begin{align}
  Z_{a^{\otimes k}}(\beta)
  &=\sum_{i_1,\ldots,i_k}
    (a_{i_1}\cdots a_{i_k})^\beta \notag\\
  &=\left(\sum_i a_i^\beta\right)^k
   =Z_a(\beta)^k,
  \qquad
  \log Z_{a^{\otimes k}}(\beta)=k\log Z_a(\beta).
  \label{eq:tensor-partition}
\end{align}
If \(f^{\otimes k}\impl g^{\otimes n}\), applying the power sum at every
\(\beta\geq0\) gives
\[
  Z_f(\beta)^k\leq Z_g(\beta)^n,
  \qquad
  \frac{k}{n}\leq
  \frac{\log Z_g(\beta)}{\log Z_f(\beta)}.
\]
Thus Cartesian products become ordinary scalar multiplication after taking
\(\log Z\).  Passing to the asymptotic optimum gives the upper bound in the
following formula.

The asymptotic comparison is governed by
\begin{equation}
  \label{eq:rate}
  C(g\mid f)
  =\inf_{\beta\geq0}
    \frac{\log Z_g(\beta)}{\log Z_f(\beta)},
\end{equation}
together with the endpoint
\[
  \lim_{\beta\to\infty}
  \frac{\log Z_g(\beta)}{\log Z_f(\beta)}
  =\frac{\log\max_j g_j}{\log\max_i f_i}.
\]
Necessity follows by applying every power sum to an implementation.
Asymptotic sufficiency follows by sorting the product fibers and applying the
usual exponential, or large-deviation, estimates to their ranks.  In this
way all componentwise constraints are represented by the one-parameter
family \(Z_a(\beta)\).

Put
\[
  \epsilon_i=-\log a_i,\qquad
  p_i(T)
  =\frac{e^{-\epsilon_i/T}}{\sum_j e^{-\epsilon_j/T}}
  =\frac{a_i^{1/T}}{Z_a(1/T)}.
\]
These are the Gibbs probabilities at physical temperature \(T\).  Their mean
energy and entropy are
\[
  E_a(T)=\sum_i p_i(T)\epsilon_i,\qquad
  H_a(T)=-\sum_i p_i(T)\log p_i(T).
\]
Writing \(\beta=1/T\), they satisfy
\(\log Z_a(\beta)=H_a(T)-E_a(T)/T\).  Moreover,
\[
 H_{a^{\otimes k}}(T)=kH_a(T),\qquad
 E_{a^{\otimes k}}(T)=kE_a(T),
\]
because the Gibbs distribution of the Cartesian power is the product
distribution.  Eliminating \(T\) gives
\[
 E_{a^{\otimes k}}(H)=kE_a(H/k),
\]
so taking Cartesian powers is a homothety by \(k\) in the
\((E,H)\)-plane.

Geometrically,
\(\log Z_a(\beta)=H_a(T)-\beta E_a(T)\) is the offset of the supporting
line with normal \((-\beta,1)\) to the convex Gibbs region \(\mathcal R_a\).
Therefore formula \eqref{eq:rate} is equivalently
\[
 C(g\mid f)
 =\sup\{c\geq0:c\,\mathcal R_f\subseteq\mathcal R_g\}.
\]
In particular,
\(C(g\mid f)\Gamma_f\) fits in the convex Gibbs region bounded by
\(\Gamma_g\).  Equivalently, the tight homothetic copy of \(g\) embracing
\(f\) is \(C(g\mid f)^{-1}\Gamma_g\).

\section{The pair
\texorpdfstring{\(\sig{2,2}\) and \(\sig{3,1}\)}
{{2,2} and {3,1}}}

Let \(g=\sig{2,2}\) and \(f=\sig{3,1}\).  Every fiber of
\(g^{\otimes n}\) has size \(2^n\).  The largest fiber of
\(f^{\otimes k}\) has size \(3^k\), and the remaining rank constraint is
weaker at the optimum.  Hence
\[
  C(\sig{2,2}\mid\sig{3,1})
  =\frac{\log2}{\log3}
  =0.6309297536\ldots.
\]
The reverse direction is limited at a finite temperature:
\begin{align*}
  C(\sig{3,1}\mid\sig{2,2})
  &=\min_{\beta\geq0}
    \frac{\log(3^\beta+1)}{(\beta+1)\log2} \\
  &=0.9653844417\ldots,
  \qquad
  \beta_*=0.4036799030\ldots,\quad
  T_*=\beta_*^{-1}=2.47721027\ldots.
\end{align*}
The large asymmetry is visible in Figure~\ref{fig:pair}.  Solid lines are the
original Gibbs boundaries.  In each panel the other signature is enlarged by
the reciprocal directed rate; only this resulting tight boundary is dotted.
The thin horizontal line joins the \(T=\infty\) endpoint to \(E=0\).  When the
\(T=0\) endpoint has residual entropy \(H(0)>0\), a thin vertical line joins
that endpoint to \(H=0\); no vertical line is needed when \(H(0)=0\).
Endpoint closures of homothetically scaled boundaries are thin and dotted,
matching the scaled curve; endpoint closures of original boundaries are thin
and solid.  The \(85\%\)-transparent fills extend horizontally from each
boundary to \(E=0\), displaying the closed convex Gibbs regions.  Since
\(\sig{2,2}\) has two equal energy levels, its canonical Gibbs boundary
itself collapses to the single point
\((E,H)=(-\log2,\log2)\).

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/exchange-homotheties_2-2_3-1.pdf}
  \caption{The two directed homotheties for \(\sig{2,2}\) and
  \(\sig{3,1}\).  Thin horizontal lines close the \(T=\infty\) endpoints.
  Vertical lines close only \(T=0\) endpoints with \(H(0)>0\).
  Both horizontal and vertical closures are dotted for homothetically scaled
  boundaries; translucent fills extend to \(E=0\).  The factors are
  \(1/C(\sig{3,1}\mid\sig{2,2})=1.03585676\ldots\) on the left and
  \(1/C(\sig{2,2}\mid\sig{3,1})=\log3/\log2\) on the right.}
  \label{fig:pair}
\end{figure}

\section{A two-sided finite-temperature tangency}

Finite-temperature contact can determine both directed rates.  Consider
\[
  a=\sig{6,5,2,1},
  \qquad
  b=\sig{6,4,3,2}.
\]
Because \(a\) and \(b\) have the same length and the same largest fiber,
both endpoint comparisons are equal to one:
\[
 \frac{\log Z_a(0)}{\log Z_b(0)}=1,
 \qquad
 \lim_{\beta\to\infty}
 \frac{\log Z_a(\beta)}{\log Z_b(\beta)}
 =\frac{\log6}{\log6}=1.
\]
Nevertheless, the two directed minima are strictly smaller than one and occur
at finite positive temperatures:
\begin{center}
\begin{tabular}{@{}lrrr@{}}
\toprule
Direction & \(C\) & \(T_*\) & \(\beta_*=1/T_*\) \\
\midrule
\(C(a\mid b)\) &
\(0.9664896992\) & \(1.7970865612\) & \(0.5564562229\) \\
\(C(b\mid a)\) &
\(0.9786567233\) & \(0.2459449336\) & \(4.0659508025\) \\
\bottomrule
\end{tabular}
\end{center}
At an interior minimizer of
\(R(\beta)=\log Z_g(\beta)/\log Z_f(\beta)\), the equation
\(R'(\beta_*)=0\), together with \(\log Z=H-\beta E\), gives
\[
  E_g(T_*)=C(g\mid f)E_f(T_*),
  \qquad
  H_g(T_*)=C(g\mid f)H_f(T_*).
\]
Thus the scaled Gibbs boundary and the target boundary meet at the same point.
Moreover
\[
  \frac{dH}{dE}=\frac{1}{T_*}=\beta_*,
\]
which is invariant under homothety, so their tangent lines also agree.  Both
contacts in Figure~\ref{fig:finite-temperature} are therefore genuine
finite-temperature tangencies rather than endpoint contacts.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/exchange-homotheties_6-5-2-1_6-4-3-2.pdf}
  \caption{Two-sided finite-temperature tangency.  The left panel scales
  \(b\) by \(1/C(b\mid a)\) to embrace \(a\); the right panel scales \(a\)
  by \(1/C(a\mid b)\) to embrace \(b\).  Crosses mark the interior contact
  points.  Thin dotted horizontal lines close the scaled regions at their
  \(T=\infty\) endpoints; translucent fills extend to \(E=0\).}
  \label{fig:finite-temperature}
\end{figure}

\section{A cyclic strict comparison}
\label{sec:strict-cycle}

\begin{definition}
For signatures \(a,b\), define
\[
  a\prec b
  \quad\Longleftrightarrow\quad
  C(a\mid b)<C(b\mid a).
\]
\end{definition}

This is a natural pairwise comparison: \(b\) is declared more complex when
it converts into \(a\) more efficiently than \(a\) converts into \(b\).
Nevertheless, it is not a strict linear order.

Set
\[
  f_1=\sig{5,3},\qquad
  f_2=\sig{3,1,1},\qquad
  f_3=\sig{6,1}.
\]
Equation \eqref{eq:rate} gives the comparisons in
Table~\ref{tab:cycle}.

\begin{table}[ht]
  \centering
  \caption{Directed rates producing a strict cycle.}
  \label{tab:cycle}
  \begin{tabular}{@{}cccc@{}}
    \toprule
    Pair & Forward rate & Reverse rate & Conclusion \\
    \midrule
    \(f_1,f_2\) & \(C(f_1\mid f_2)=0.630929754\)
                & \(C(f_2\mid f_1)=0.670211928\) & \(f_1\prec f_2\) \\
    \(f_2,f_3\) & \(C(f_2\mid f_3)=0.613147193\)
                & \(C(f_3\mid f_2)=0.630929754\) & \(f_2\prec f_3\) \\
    \(f_3,f_1\) & \(C(f_3\mid f_1)=0.897931851\)
                & \(C(f_1\mid f_3)=0.898244402\) & \(f_3\prec f_1\) \\
    \bottomrule
  \end{tabular}
\end{table}

Thus
\[
  f_1\prec f_2,\qquad f_2\prec f_3,\qquad f_3\prec f_1.
\]
A strict linear order must be transitive, so this three-cycle proves that
\(\prec\) is not a linear order.  Figure~\ref{fig:cycle} shows all three
pairwise comparisons geometrically.  In the panel targeting \(f_i\), the two
dotted curves are \(C(f_j\mid f_i)^{-1}\Gamma_{f_j}\); each is the tight
homothetic boundary embracing \(\Gamma_{f_i}\).

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/exchange-homotheties_cycle-5-3_3-1-1_6-1.pdf}
  \caption{The cyclic example.  Solid and dotted boundaries are original and
  scaled, respectively.  Horizontal \(T=\infty\) closures and
  85\%-transparent fills extending to \(E=0\) display target containment.}
  \label{fig:cycle}
\end{figure}

\section{A thermodynamic analogy and its limits}

There is an appealing physical reading of the construction.  Regard a
signature \(A=(a_i)\) as one idealized particle whose internal energy levels
are \(\epsilon_i=-\log a_i\).  The Cartesian power \(A^{\otimes k}\) is then a
system of \(k\) independent particles of type \(A\).  Likewise, a second
signature \(B\) represents a second particle type.  At an interior contact of
their homothetically scaled Gibbs boundaries,
\[
  \frac{dH_A}{dE}=\frac{dH_B}{dE}=\frac{1}{T},
\]
so the active comparison has a common temperature.  This makes the
finite-temperature tangencies above analogous to reversible contact between
two gases of particles.

The analogy must not be read as a law of spontaneous reaction.  The relation
\(\impl\) is a combinatorial possibility, not a microscopic dynamics, and
\(C\) is a conversion capacity rather than a chemical affinity.  For a
putative conversion
\[
  kA\rightleftharpoons nB
\]
put \(r=k/n\).  The definitions give the two distinct conditions
\begin{align}
  B^{\otimes n}\longrightarrow A^{\otimes k}
  &\quad\Longleftrightarrow\quad
  r\leq C(B\mid A)
  &&\text{in the asymptotic closure}, \label{eq:B-to-A}\\
  A^{\otimes k}\longrightarrow B^{\otimes n}
  &\quad\Longleftrightarrow\quad
  r\geq \frac{1}{C(A\mid B)}
  &&\text{in the asymptotic closure}. \label{eq:A-to-B}
\end{align}
Strict inequalities guarantee the corresponding direction for sufficiently
large block sizes; an exact boundary ratio need not be attained at finite
size.
Thus a single inequality involving \(C(A\mid B)\) does not choose the
direction.  In fact,
\[
  C(A\mid B)C(B\mid A)\leq1,
\]
so the two thresholds form a conversion window
\[
  C(B\mid A)\leq r\leq\frac{1}{C(A\mid B)}.
\]
Away from equality there can be ratios in this window for which neither
direction realizes that exact stoichiometry.  The width of the window is an
irreversibility loss, analogous to a bid--ask spread.  When the endpoints
coincide, the comparison is asymptotically reversible.  An interior tangency
identifies the Gibbs temperature at which the corresponding supporting
constraint is tight.

For comparison, genuine chemical equilibrium at fixed temperature and
pressure would require zero reaction affinity,
\[
  \mathcal A=k\mu_A-n\mu_B=0,
\]
in addition to equal temperature.  Here \(\mu_A,\mu_B\) are chemical
potentials.  This condition is not part of the definition of
implementation.  Consequently, the rate inequalities should be called
\emph{allowed conversion directions}, not spontaneous reaction directions.

The strict three-cycle suggests a sharper, explicitly nonequilibrium analogy.
For a strict comparison arrow
\(a\overset{\mathrm{s}}{\longrightarrow}b\), define the dimensionless
\emph{complexity affinity}
\[
  \alpha(a\to b)
  =\log\frac{C(a\mid b)}{C(b\mid a)}>0.
\]
For the cycle in Section~\ref{sec:strict-cycle}, whose arrow orientation is
\[
  f_1\overset{\mathrm{s}}{\longrightarrow}f_3
  \overset{\mathrm{s}}{\longrightarrow}f_2
  \overset{\mathrm{s}}{\longrightarrow}f_1,
\]
the total complexity-cycle affinity is
\[
\begin{split}
  \mathcal A_{\mathrm{cyc}}
  ={}&\alpha(f_1\to f_3)+\alpha(f_3\to f_2)+\alpha(f_2\to f_1)\\
  ={}&
  \log\frac{0.898244402}{0.897931851}
  +\log\frac{0.630929754}{0.613147193}
  +\log\frac{0.670211928}{0.630929754}\\
  ={}&0.089337,
  \qquad e^{\mathcal A_{\mathrm{cyc}}}=1.09345.
\end{split}
\]
If \(\alpha\) is formally read as a logarithmic forward--reverse reaction
bias, this resembles a preferred cycle in a three-component reaction network,
\[
  A\rightleftharpoons B,\qquad
  B\rightleftharpoons C,\qquad
  C\rightleftharpoons A.
\]

Such a cycle cannot describe spontaneous circulation in a closed equilibrium
gas.  Reaction affinity is the negative derivative of Gibbs energy with
respect to reaction extent
(see \cite{iupac-affinity});
because Gibbs energy is a state function, the total affinity around a closed
equilibrium cycle vanishes.  Equivalently, detailed balance imposes the
Wegscheider cycle condition, so all three directions cannot simultaneously
have strictly positive equilibrium affinity.  A nonzero cycle affinity instead
belongs naturally to an open system driven by chemostats, fuel, radiation, or
other reservoirs.  Open chemical reaction networks with emergent cycles and
nonvanishing affinities are developed by Polettini and Esposito
\cite{polettini-esposito-2014} and Rao and Esposito
\cite{rao-esposito-2016}; Schmiedl and Seifert
\cite{schmiedl-seifert-2007} give a stochastic-thermodynamic three-species
cyclic example.

This identification remains structural rather than physical.  The quantities
\(C(a\mid b)\) are conversion capacities, not kinetic rate constants; the
three pairwise infima may occur at different temperatures; and the signature
model supplies neither reaction kinetics nor conservation laws, stoichiometric
closure, or external work accounting.  Hence a strict comparison cycle is a
\emph{candidate nonequilibrium reaction cycle}: it gives an orientation and a
formal cycle affinity, but by itself predicts neither a sustained current nor
chemical oscillations.

A constant reaction-energy term can nevertheless be represented cleanly.
For
\[
  kA\rightleftharpoons nB+q
\]
shift every level of \(B\) by \(q/n\):
\[
  \widetilde\epsilon_{B,j}=\epsilon_{B,j}+\frac qn.
\]
Then
\[
  \widetilde E_B(H)=E_B(H)+\frac qn,\qquad
  \widetilde Z_B(T)=e^{-q/(nT)}Z_B(T),
\]
while \(H_B(T)\) and the tangent slope \(dH_B/dE=1/T\) are unchanged.  Hence
the \(n\)-particle energy region moves horizontally by \(q\).  This bookkeeping
is valid when \(q\) is a fixed energy offset carried by an explicit work or
storage degree of freedom.  Heat passed to a bath also carries entropy and
cannot, in general, be modeled by a horizontal shift alone.

Several physical effects are deliberately absent: interactions, quantum
symmetrization, translational and volume degrees of freedom, mixing entropy
between particle types, conservation laws, reaction kinetics, activation
barriers, and coupling to a heat or particle reservoir.  The logarithmic
energies are dimensionless (\(k_B=1\)), and relative energy offsets between
particle types must be fixed because a conversion may change particle
number.  Subject to these simplifications, the construction suggests a
broader question: use homothetic containment of Gibbs regions to compare the
complexity of general Gibbs systems.

The supporting function
\[
  \Psi_A(T)=\log Z_A(T)=-\frac{F_A(T)}{T}
\]
is the Massieu potential, or free entropy.  Thus
\[
  C(A\mid B)=\inf_{T>0}\frac{\Psi_A(T)}{\Psi_B(T)}
\]
is the largest factor \(c\) for which
\(\Psi_A(T)\geq c\Psi_B(T)\) simultaneously at every temperature.  It may be
read as an all-temperature, or worst-temperature, conversion rate; an
interior minimizing \(T_*\) is the bottleneck temperature.  The closest
established framework is the resource theory without a fixed background
temperature developed by Sparaciari, Oppenheim, and Fritz
(see \cite{sparaciari-oppenheim-fritz-2017}).  There, asymptotically many
noninteracting systems are classified by energy and entropy, and conversion
rates are read from a two-dimensional energy--entropy diagram.  Our
construction specializes this idea to complete Gibbs boundaries and requires
the comparison at all temperatures rather than relative to one prescribed
heat bath.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.78\textwidth]{figures/relativistic-radiation-energy-entropy.pdf}
  \caption{A real exact homothety in the ultrarelativistic limit.  At the same
  temperature, the electron--positron sector has \(7/4\) times the energy and
  entropy densities of the photon sector.  The dotted homothetic photon curve
  coincides with the electron--positron curve.}
  \label{fig:radiation-sectors}
\end{figure}

A simple real system realizes the homothety exactly.  For an
ultrarelativistic equilibrium sector \(X\) with effective number \(g_X\) of
internal degrees of freedom, in natural units and fixed volume,
\[
  E_X(T)=\frac{\pi^2}{30}g_XVT^4,\qquad
  H_X(T)=\frac{2\pi^2}{45}g_XVT^3.
\]
Consequently, two such sectors \(A,B\) obey
\[
  E_A(T)=\frac{g_A}{g_B}E_B(T),\qquad
  H_A(T)=\frac{g_A}{g_B}H_B(T)
\]
at every temperature: their entire energy--entropy boundaries are
homothetic, with \(C=g_A/g_B\).  Photons have \(g_\gamma=2\), whereas an
ultrarelativistic electron--positron sector has the fermionic effective value
\(g_{e^\pm}=(7/8)\times4=7/2\).  Hence its factor relative to photons is
\(7/4\), as shown in Figure~\ref{fig:radiation-sectors}.  The early-universe
reaction \(e^-+e^+\rightleftharpoons2\gamma\) maintained thermal and chemical
equilibrium in the relativistic regime; when the temperature fell below the
electron mass, annihilation transferred entropy to the photon bath.  Detailed
calculations confirm that most annihilation occurred near equilibrium and
that the process affects big-bang nucleosynthesis and the effective
relativistic degrees of freedom (see \cite{thomas-et-al-2020}).

For arbitrary Gibbs systems this should initially be viewed as a proposed
complexity comparison: unlike finite-map signatures, its operational
interpretation and the treatment of infinite spectra require separate
analysis.

\section{Conclusion}

Fiber signatures retain only a very small part of a finite map, yet their
Cartesian products already produce a nontrivial asymptotic resource theory.
The operational rate \(C(g\mid f)\), the power-sum formula, and the
energy--entropy homotheties are three views of the same comparison.  The
example above also warns against compressing the two directed rates into a
single global ranking: pairwise exchange advantage can be cyclic.
The thermodynamic analogy suggests using the same Gibbs-region comparison
for broader systems while keeping conversion capacity distinct from physical
reaction affinity.

\begin{thebibliography}{99}
\raggedright

\bibitem{coecke-fritz-spekkens-2016}
B.~Coecke, T.~Fritz, and R.~W.~Spekkens,
``A mathematical theory of resources,''
\emph{Information and Computation} \textbf{250}, 59--86 (2016).
\href{https://doi.org/10.1016/j.ic.2016.02.008}
{doi:10.1016/j.ic.2016.02.008}.

\bibitem{fritz-2017}
T.~Fritz,
``Resource convertibility and ordered commutative monoids,''
\emph{Math. Struct. Comput. Sci.} \textbf{27}, 850--938 (2017).
\href{https://doi.org/10.1017/S0960129515000444}
{doi:10.1017/S0960129515000444}.

\bibitem{brattka-gherardi-pauly-2021}
V.~Brattka, G.~Gherardi, and A.~Pauly,
``Weihrauch complexity in computable analysis,''
in \emph{Handbook of Computability and Complexity in Analysis},
pp.~367--417 (Springer, Cham, 2021).
\href{https://doi.org/10.1007/978-3-030-59234-9_11}
{doi:10.1007/978-3-030-59234-9\_11}.

\bibitem{strassen-1988}
V.~Strassen,
``The asymptotic spectrum of tensors,''
\emph{J. Reine Angew. Math.} \textbf{384}, 102--152 (1988).
\href{https://doi.org/10.1515/crll.1988.384.102}
{doi:10.1515/crll.1988.384.102}.

\bibitem{christandl-vrana-zuiddam-2021}
M.~Christandl, P.~Vrana, and J.~Zuiddam,
``Barriers for fast matrix multiplication from irreversibility,''
\emph{Theory Comput.} \textbf{17}(2), 1--32 (2021).
\href{https://doi.org/10.4086/toc.2021.v017a002}
{doi:10.4086/toc.2021.v017a002}.

\bibitem{jensen-2019}
A.~Kj{\ae}rulff Jensen,
``Asymptotic majorization of finite probability distributions,''
\emph{IEEE Trans. Inf. Theory} \textbf{65}, 8131--8139 (2019).
\href{https://doi.org/10.1109/TIT.2019.2922627}
{doi:10.1109/TIT.2019.2922627}.

\bibitem{cao-et-al-2024}
M.~X.~Cao, N.~Ramakrishnan, M.~Berta, and M.~Tomamichel,
``Channel simulation: finite blocklengths and broadcast channels,''
\emph{IEEE Trans. Inf. Theory} \textbf{70}, 6780--6808 (2024).
\href{https://doi.org/10.1109/TIT.2024.3445998}
{doi:10.1109/TIT.2024.3445998};
\href{https://arxiv.org/abs/2212.11666}{arXiv:2212.11666}.

\bibitem{iupac-affinity}
International Union of Pure and Applied Chemistry,
``Affinity of reaction,''
\emph{Compendium of Chemical Terminology (the Gold Book)}, 5th ed.,
online version 5.0.0 (2025).
\href{https://doi.org/10.1351/goldbook.A00178}
{doi:10.1351/goldbook.A00178}.

\bibitem{polettini-esposito-2014}
M.~Polettini and M.~Esposito,
``Irreversible thermodynamics of open chemical networks I:
emergent cycles and broken conservation laws,''
\emph{J. Chem. Phys.} \textbf{141}, 024117 (2014).
\href{https://doi.org/10.1063/1.4886396}{doi:10.1063/1.4886396}.

\bibitem{rao-esposito-2016}
R.~Rao and M.~Esposito,
``Nonequilibrium thermodynamics of chemical reaction networks:
wisdom from stochastic thermodynamics,''
\emph{Phys. Rev. X} \textbf{6}, 041064 (2016).
\href{https://doi.org/10.1103/PhysRevX.6.041064}
{doi:10.1103/PhysRevX.6.041064}.

\bibitem{schmiedl-seifert-2007}
T.~Schmiedl and U.~Seifert,
``Stochastic thermodynamics of chemical reaction networks,''
\emph{J. Chem. Phys.} \textbf{126}, 044101 (2007).
\href{https://doi.org/10.1063/1.2428297}{doi:10.1063/1.2428297}.

\bibitem{sparaciari-oppenheim-fritz-2017}
C.~Sparaciari, J.~Oppenheim, and T.~Fritz,
``A resource theory for work and heat,''
\emph{Phys. Rev. A} \textbf{96}, 052112 (2017).
\href{https://doi.org/10.1103/PhysRevA.96.052112}
{doi:10.1103/PhysRevA.96.052112}.

\bibitem{thomas-et-al-2020}
L.~C.~Thomas, T.~Dezen, E.~B.~Grohs, and C.~T.~Kishimoto,
``Electron-positron annihilation freeze-out in the early universe,''
\emph{Phys. Rev. D} \textbf{101}, 063507 (2020).
\href{https://doi.org/10.1103/PhysRevD.101.063507}
{doi:10.1103/PhysRevD.101.063507}.

\end{thebibliography}

\appendix
\section{Unrestricted input and output maps}

Suppose that the maps \(h_{\mathrm{in}}\) and \(h_{\mathrm{out}}\) in the
definition of implementation are allowed to be arbitrary.  Then \(b\) can be
implemented by \(a\) exactly when
\[
  |Y_b|\leq |Y_a|.
\]
Indeed, arbitrary \(h_{\mathrm{in}}\) may send every element of a fiber of
\(b\) to one chosen input of \(a\), so the fiber sizes impose no constraint.
Conversely, a deterministic \(h_{\mathrm{out}}\) cannot produce more distinct
outputs than \(a\) has.

For Cartesian powers the condition is therefore
\[
  |Y_b|^k\leq |Y_a|^n,
  \qquad
  k_{\max}(n)
  =\left\lfloor
    n\frac{\log|Y_a|}{\log|Y_b|}
   \right\rfloor.
\]
Consequently, when \(|Y_a|,|Y_b|>1\),
\[
  \boxed{\displaystyle
  C(a\mid b)=\frac{\log|Y_a|}{\log|Y_b|}}.
\]
This is the same first-order form as assisted point-to-point channel
interconversion \cite{cao-et-al-2024}: a surjective deterministic channel with
output set \(Y\) has capacity \(\log|Y|\), so the displayed rate is the ratio
of the two channel capacities.  The unrestricted variant therefore sees only
ordinary communication capacity, whereas the injective and bijective
processors in the main theory retain the individual fiber sizes.

Thus \(C(a\mid b)C(b\mid a)=1\), and the comparison from
Definition~3 reduces to
\[
  a\prec b
  \quad\Longleftrightarrow\quad
  |Y_a|<|Y_b|.
\]
It is a total preorder on signatures, with equal-size output sets tied, and
becomes a linear order after identifying signatures having the same
\(|Y|\).  In partition-function language only the endpoint
\(Z_a(0)=|Y_a|\) remains; the fiber sizes and the rest of the Gibbs curve play
no role.  If \(|Y_b|=1\), then \(C(a\mid b)=+\infty\); if
\(|Y_a|=1<|Y_b|\), then \(C(a\mid b)=0\).

\section{The first 69 non-special signatures}

Singleton signatures \(\sig{N}\) and all-ones signatures
\(\sig{1,\ldots,1}\) form exceptional infinite families and are omitted from
this finite list.  To exhaust the remaining signatures without arbitrary
length or entry caps, put
\[
  \mathcal S_B
  =\{a:|a|\geq2,\ a_1>1,\ a_1+2|a|\leq B\}.
\]
Every non-special signature belongs to \(\mathcal S_B\) for all sufficiently
large \(B\).  For each \(B\), we contract the strongly connected components
of the strict comparison graph and use the deterministic topological order
described in the analysis code.  At the stated numerical screening precision,
the first 69 entries are identical for \(B=18\), \(B=19\), and \(B=20\);
Table~\ref{tab:first-69-signatures} records this stabilized cutoff.  Its
displayed rates are then recomputed with the high-accuracy persistent cache.

Because \(\prec\) is not transitive, the resulting numbered list is not a
linear extension of every pairwise comparison.  In
Table~\ref{tab:signature-order-exceptions}, a row for \(x\) records every
earlier numbered signature \(a\) for which \(x\prec a\), together with the two
directed rates.  Thus each listed entry satisfies
\[
  C(x\mid a)<C(a\mid x)
\]
despite \(a\) appearing before \(x\).

\input{appendix_b_signatures.tex}

\subsection{The global comparison clusters}

On the infinite family of non-special integer signatures, we distinguish a
non-strict and a strict comparison arrow:
\[
  a\longrightarrow b
  \quad\Longleftrightarrow\quad
  C(a\mid b)\geq C(b\mid a),
  \qquad
  a\overset{\mathrm{s}}{\longrightarrow}b
  \quad\Longleftrightarrow\quad
  C(a\mid b)>C(b\mid a).
\]
Thus an arrow points from the greater element toward the smaller one, like
the \(>\) symbol; equivalently,
\(a\overset{\mathrm{s}}{\longrightarrow}b\) means \(b\prec a\).
Equality produces two opposite non-strict arrows and no strict arrow.  Define
the corresponding clusters as the strongly connected components
\[
  \operatorname{Cl}_{\geq}(a)
  =\{b:a\longrightarrow^{*}b
          \ \text{and}\ b\longrightarrow^{*}a\},
\]
and analogously define \(\operatorname{Cl}_{>}(a)\) using only
\(\overset{\mathrm{s}}{\longrightarrow}\).  Reversing the orientation used
for the arrows does not change either strongly connected component.

This is an infinite-graph question: a strongly connected component of a
finite induced subgraph need not equal the intersection of the global
component with that subgraph, because paths may pass through signatures
outside the truncation.  In particular, the 69-prefix give misleadingly small
components.

Calculations on the nested finite shells \(\mathcal S_B\) suggest the following
non-strict global description, which we leave as a conjecture:
\[
  \operatorname{Cl}_{\geq}\bigl(\sig{3,1,1}\bigr)
  =
  \{a:|a|\geq2,\ a_1>1\}
  \setminus\{\sig{2,1},\sig{2,2}\}.
\]
For example, the component contains 112 of the 129 signatures in
\(\mathcal S_{12}\), and 2498 of the 2560 signatures in
\(\mathcal S_{18}\).  Across the tested shells \(12\leq B\leq18\), these
finite components do not stabilize: signatures near the boundary of a smaller
shell join through paths appearing in larger shells.  Thus no finite list is
asserted here.

The strict cluster appears to have an equally simple description.  Put
\[
\begin{split}
  \mathcal E_{\mathrm{s}}=\{&
    \sig{2,1},\sig{2,2},
    \sig{3,1},\sig{3,2},\sig{3,3},
    \sig{4,1},\sig{4,2},\sig{4,3},\\
    &\sig{2,1,1},\sig{2,2,1},\sig{2,2,2},
    \sig{2,1,1,1}\}.
\end{split}
\]
Our strict-cluster conjecture is
\[
  \operatorname{Cl}_{>}\bigl(\sig{3,1,1}\bigr)
  =
  \{a:|a|\geq2,\ a_1>1\}\setminus\mathcal E_{\mathrm{s}}.
\]
As finite-shell evidence, the strict component in \(\mathcal S_{20}\)
contains 6719 of its 6738 signatures.  Its intersection with
\(\mathcal S_{19}\) is exactly
\(\mathcal S_{19}\setminus\mathcal E_{\mathrm{s}}\); the remaining seven
excluded signatures are all new boundary points with
\(a_1+2|a|=20\).  This pattern is evidence rather than a proof that later
shells absorb every boundary exception and never absorb a member of
\(\mathcal E_{\mathrm{s}}\).

Here is one explicit strict cycle, computed inside \(\mathcal S_{14}\) with
comparison tolerance \(2\cdot10^{-6}\).  The shortest strict path from
\(\sig{9,1}\) to \(\sig{3,2,2,2}\), in the arrow orientation above, is
\[
  \sig{9,1}
  \overset{\mathrm{s}}{\longrightarrow}\sig{3,3,3}
  \overset{\mathrm{s}}{\longrightarrow}\sig{3,1,1,1}
  \overset{\mathrm{s}}{\longrightarrow}\sig{8,4}
  \overset{\mathrm{s}}{\longrightarrow}\sig{10,1}
  \overset{\mathrm{s}}{\longrightarrow}\sig{3,2,2,2}.
\]
Every row of Table~\ref{tab:strict-path-forward} satisfies
\(C(a\mid b)>C(b\mid a)\).

\begin{table}[htbp]
  \centering
  \caption{Strict path from \(\sig{9,1}\) to \(\sig{3,2,2,2}\).}
  \label{tab:strict-path-forward}
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Strict edge \(a\overset{\mathrm{s}}{\longrightarrow}b\)
      & \(C(a\mid b)\) & \(C(b\mid a)\) \\
    \midrule
    \(\sig{9,1}\overset{\mathrm{s}}{\longrightarrow}\sig{3,3,3}\)
      & 0.630929754 & 0.500000000 \\
    \(\sig{3,3,3}\overset{\mathrm{s}}{\longrightarrow}\sig{3,1,1,1}\)
      & 0.792481250 & 0.753940561 \\
    \(\sig{3,1,1,1}\overset{\mathrm{s}}{\longrightarrow}\sig{8,4}\)
      & 0.527921767 & 0.500000000 \\
    \(\sig{8,4}\overset{\mathrm{s}}{\longrightarrow}\sig{10,1}\)
      & 0.903089987 & 0.899142666 \\
    \(\sig{10,1}\overset{\mathrm{s}}{\longrightarrow}\sig{3,2,2,2}\)
      & 0.500000000 & 0.477121255 \\
    \bottomrule
  \end{tabular}
\end{table}

The shortest strict return path has only two edges:
\[
  \sig{3,2,2,2}
  \overset{\mathrm{s}}{\longrightarrow}\sig{7,5}
  \overset{\mathrm{s}}{\longrightarrow}\sig{9,1}.
\]

\begin{table}[htbp]
  \centering
  \caption{Strict return path from \(\sig{3,2,2,2}\) to \(\sig{9,1}\).}
  \label{tab:strict-path-return}
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Strict edge \(a\overset{\mathrm{s}}{\longrightarrow}b\)
      & \(C(a\mid b)\) & \(C(b\mid a)\) \\
    \midrule
    \(\sig{3,2,2,2}\overset{\mathrm{s}}{\longrightarrow}\sig{7,5}\)
      & 0.564574819 & 0.500000000 \\
    \(\sig{7,5}\overset{\mathrm{s}}{\longrightarrow}\sig{9,1}\)
      & 0.885621875 & 0.871797909 \\
    \bottomrule
  \end{tabular}
\end{table}

\clearpage
\subsection{A length-three strict cycle}

Strict cyclicity is not caused by comparing signatures of different lengths.
An exhaustive high-accuracy evaluation of the 55 non-special length-three
signatures with entries at most \(6\) finds no strict three-cycle at comparison
tolerance \(2\cdot10^{-6}\).  The first maximum entry for which the bounded
search finds one is \(7\):
\[
  \sig{6,3,3}
  \overset{\mathrm{s}}{\longrightarrow}\sig{7,2,1}
  \overset{\mathrm{s}}{\longrightarrow}\sig{6,5,1}
  \overset{\mathrm{s}}{\longrightarrow}\sig{6,3,3}.
\]
Table~\ref{tab:length-three-cycle} gives both directed rates on every edge.

\begin{table}[htbp]
  \centering
  \caption{A strict three-cycle of length-three signatures.}
  \label{tab:length-three-cycle}
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Strict edge \(a\overset{\mathrm{s}}{\longrightarrow}b\)
      & \(C(a\mid b)\) & \(C(b\mid a)\) & difference \\
    \midrule
    \(\sig{6,3,3}\overset{\mathrm{s}}{\longrightarrow}\sig{7,2,1}\)
      & 0.920782221 & 0.910910734 & 0.009871488 \\
    \(\sig{7,2,1}\overset{\mathrm{s}}{\longrightarrow}\sig{6,5,1}\)
      & 0.924704131 & 0.920782221 & 0.003921910 \\
    \(\sig{6,5,1}\overset{\mathrm{s}}{\longrightarrow}\sig{6,3,3}\)
      & 0.975533650 & 0.959474358 & 0.016059292 \\
    \bottomrule
  \end{tabular}
\end{table}

\end{document}
